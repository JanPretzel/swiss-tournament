export interface Theme {
  teamDimensions: number;
  matchHeight: number;
  matchWidth: number;
  matchPadding: number;
  bucketPadding: number;
}

export default {
  teamDimensions: 40,
  matchHeight: 40,
  matchWidth: 140,
  matchPadding: 15,
  bucketPadding: 15,
} as Theme;
